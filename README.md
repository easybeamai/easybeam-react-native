# EasyBeam Chat Service

A React Native package for interacting with Easybeam chat services, supporting both streaming and non-streaming responses.

## Installation

```bash
npm install easybeam-chat-service
```
